name: Apply Canvas inventory upgrade

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  retired:
    runs-on: ubuntu-latest
    steps:
      - run: echo "One-time Canvas inventory upgrade is complete."
