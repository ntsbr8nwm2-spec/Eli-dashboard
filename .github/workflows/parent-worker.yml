name: Parent dashboard worker

on:
  workflow_dispatch:
  schedule:
    - cron: '3,8,13,18,23,28,33,38,43,48,53,58 * * * *'
  push:
    paths:
      - 'parent-worker-client.mjs'
      - 'school-monitor.mjs'
      - 'canvas-only.mjs'
      - 'canvas-guard.mjs'
      - 'focus-news-v2.mjs'
      - 'sanitize-dashboard.mjs'
      - '.github/workflows/parent-worker.yml'

permissions:
  contents: read
  id-token: write

concurrency:
  group: parent-dashboard-worker
  cancel-in-progress: false

jobs:
  monitor:
    runs-on: ubuntu-latest
    timeout-minutes: 18
    env:
      PARENT_WORKDIR: /tmp/parent-school-run

    steps:
      - name: Check out monitor engine
        uses: actions/checkout@v4

      - name: Set up Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '22'

      - name: Lease next private parent dashboard
        id: lease
        run: node parent-worker-client.mjs lease

      - name: Prepare isolated student workspace
        if: env.NO_PARENT_JOB != 'true'
        run: |
          cp school-monitor.mjs canvas-only.mjs canvas-guard.mjs focus-news-v2.mjs sanitize-dashboard.mjs "$PARENT_WORKDIR"/
          chmod 700 "$PARENT_WORKDIR"
          chmod 600 "$PARENT_WORKDIR"/*.json || true

      - name: Install Playwright in isolated workspace
        if: env.NO_PARENT_JOB != 'true'
        run: |
          npm install --prefix "$PARENT_WORKDIR" --no-save playwright@1.55.0
          "$PARENT_WORKDIR/node_modules/.bin/playwright" install --with-deps chromium

      - name: Run Focus grades through student BCPS login
        if: env.NO_PARENT_JOB != 'true'
        id: grades
        working-directory: /tmp/parent-school-run
        continue-on-error: true
        run: node school-monitor.mjs 2>grade-stderr.txt

      - name: Print safe Focus diagnostics
        if: env.NO_PARENT_JOB != 'true' && steps.grades.outcome == 'failure'
        working-directory: /tmp/parent-school-run
        run: |
          echo "--- SAFE FOCUS AUTH DIAGNOSTICS ---"
          if [ -f auth-debug.json ]; then cat auth-debug.json; else echo "No auth-debug.json was produced."; fi
          echo "--- END SAFE FOCUS AUTH DIAGNOSTICS ---"

      - name: Save previous Canvas assignments
        if: env.NO_PARENT_JOB != 'true' && steps.grades.outcome == 'success'
        working-directory: /tmp/parent-school-run
        run: node canvas-guard.mjs before

      - name: Run Canvas assignments
        if: env.NO_PARENT_JOB != 'true' && steps.grades.outcome == 'success'
        id: canvas
        working-directory: /tmp/parent-school-run
        continue-on-error: true
        run: node canvas-only.mjs

      - name: Confirm empty Canvas result
        if: env.NO_PARENT_JOB != 'true' && steps.canvas.outcome == 'success'
        working-directory: /tmp/parent-school-run
        run: node canvas-guard.mjs after

      - name: Sanitize base dashboard data
        if: env.NO_PARENT_JOB != 'true' && steps.grades.outcome == 'success' && steps.canvas.outcome == 'success'
        working-directory: /tmp/parent-school-run
        run: node sanitize-dashboard.mjs

      - name: Publish grades Canvas and fallback activity
        if: env.NO_PARENT_JOB != 'true' && steps.grades.outcome == 'success' && steps.canvas.outcome == 'success'
        run: node parent-worker-client.mjs publish

      - name: Send grade change SMS notification
        if: env.NO_PARENT_JOB != 'true' && steps.grades.outcome == 'success' && steps.canvas.outcome == 'success'
        continue-on-error: true
        run: node parent-worker-client.mjs notify

      - name: Run Focus activity
        if: env.NO_PARENT_JOB != 'true' && steps.grades.outcome == 'success' && steps.canvas.outcome == 'success'
        id: news
        working-directory: /tmp/parent-school-run
        continue-on-error: true
        run: node focus-news-v2.mjs

      - name: Publish richer Focus activity
        if: env.NO_PARENT_JOB != 'true' && steps.news.outcome == 'success'
        run: node parent-worker-client.mjs publish

      - name: Record failed private run
        if: always() && env.NO_PARENT_JOB != 'true' && (steps.grades.outcome == 'failure' || steps.canvas.outcome == 'failure')
        env:
          WORKER_ERROR: method=student_sso; grades=${{ steps.grades.outcome }}; canvas=${{ steps.canvas.outcome }}; news=${{ steps.news.outcome }}
        run: node parent-worker-client.mjs fail

      - name: Mark collector failure
        if: env.NO_PARENT_JOB != 'true' && (steps.grades.outcome == 'failure' || steps.canvas.outcome == 'failure')
        run: exit 1
